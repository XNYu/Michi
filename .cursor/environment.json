{
  "name": "Michi",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "dev",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\"; npm run dev",
      "description": "Michi backend (http://localhost:3000) and Vite frontend (http://localhost:3001). Cursor CLI is on PATH as ~/.local/bin/agent. Open the frontend URL in the browser."
    }
  ],
  "ports": [
    {
      "name": "backend",
      "port": 3000
    },
    {
      "name": "frontend",
      "port": 3001
    }
  ]
}
